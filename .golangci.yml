version: 2

linters:
  enable:
    - asasalint
    - asciicheck
    - bidichk
    - decorder
    - durationcheck
    - errcheck
    - errorlint
    - exptostd
    - fatcontext
    - gocheckcompilerdirectives
    - gochecksumtype
    - goconst
    - godoclint
    - gosmopolitan
    - grouper
    - iface
    - importas
    - mirror
    - misspell
    - musttag
    - noctx
    - perfsprint
    - prealloc
    - reassign
    - recvcheck
    - sloglint
    - testifylint
    - thelper
    - unconvert
    - wastedassign
    - whitespace
  settings:
    goconst:
      min-len: 4
      min-occurrences: 4
    recvcheck:
      # value String/MarshalJSON alongside pointer UnmarshalJSON is the standard
      # enum shape, not a mistake
      exclusions:
        - "*.String"

formatters:
  enable:
    - gofmt
    - goimports

run:
  timeout: 600s
